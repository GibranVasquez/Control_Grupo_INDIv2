## Agent skills

### Issue tracker

GitHub Issues on `GibranVasquez/Control_Grupo_INDIv2`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root (this repo hosts frontend, backend, and database together, not a monorepo). See `docs/agents/domain.md`.
